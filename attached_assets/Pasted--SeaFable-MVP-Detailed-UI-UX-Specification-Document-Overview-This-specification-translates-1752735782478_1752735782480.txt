# SeaFable MVP - Detailed UI/UX Specification

## 📋 Document Overview
This specification translates the MVP feature requirements into detailed UI/UX implementation guidelines for developers. Each feature is categorized by priority and includes specific implementation details.

## 🎯 Priority Framework
- **P0**: Must have for MVP (Critical)
- **P1**: Nice to have for MVP (can be simplified or dropped if needed)
- **P2**: Out of scope for MVP (Future releases)

---

## 🏠 1. Homepage Implementation

### 1.1 Hero Section with Search Bar (P0)

#### **Search Bar Component**
```tsx
// Primary search interface - most important MVP feature
<SearchBar>
  <SearchField 
    type="what" 
    placeholder="What water adventure?" 
    priority="P0"
  />
  <SearchField 
    type="where" 
    placeholder="Where?" 
    priority="P0" 
  />
  <SearchField 
    type="date" 
    placeholder="When?" 
    priority="P0"
  />
  <SearchButton>Search</SearchButton>
</SearchBar>
```

**Visual Requirements:**
- **Location**: Centered in hero section
- **Size**: Full-width on mobile, max-width 800px on desktop
- **Style**: Clean white background, rounded corners
- **Fields**: 3 main fields + search button
- **Responsive**: Stack vertically on mobile

**Functionality (P0):**
- **What Field**: 
  - Free text input for activity search
  - **P1 Enhancement**: Add suggested categories dropdown
- **Where Field**: 
  - Free text input for location
  - **P1 Enhancement**: Add suggested places based on operation areas
- **Date Field**: 
  - Simple date picker
  - **P1 Enhancement**: Add duration selector (hours/days)
- **Search Button**: Navigate to search results page

### 1.2 Activity Categories Section (P0)

#### **Activity Grid Layout**
```tsx
<ActivityGrid>
  {activities.map(activity => (
    <ActivityCard key={activity.type}>
      <ActivityIcon>{activity.icon}</ActivityIcon>
      <ActivityName>{activity.name}</ActivityName>
      <ActivityCount>{activity.count} experiences</ActivityCount>
    </ActivityCard>
  ))}
</ActivityGrid>
```

**Activities to Display (P0):**
- Sailing (⛵) - "Make yacht charter in sailing"
- Diving (🤿)
- Kitesurfing (🪁)
- Surfing (🏄‍♂️)
- Kayaking (🛶)
- Paddleboarding (🏄‍♀️)

**Visual Requirements:**
- **Layout**: 2 columns on mobile, 3 columns on tablet, 6 columns on desktop
- **Card Design**: Clean white cards with hover effects
- **Icons**: Large emoji or SVG icons
- **Spacing**: Consistent 16px gaps between cards

**Note**: No fishing included (too controversial per requirements)

### 1.3 Destination Showcase (P1)

#### **Destination Cards**
```tsx
<DestinationSection>
  <SectionTitle>Explore by Destination</SectionTitle>
  <DestinationGrid>
    {destinations.map(dest => (
      <DestinationCard>
        <DestinationImage src={dest.image} />
        <DestinationName>{dest.name}</DestinationName>
        <ExperienceCount>{dest.count} experiences</ExperienceCount>
      </DestinationCard>
    ))}
  </DestinationGrid>
</DestinationSection>
```

**Purpose**: Show coverage and operation areas
**Implementation**: Only if easy to implement
**Content**: Major coastal destinations where SeaFable operates

### 1.4 Featured Adventures Section (P1)

#### **Featured Experience Cards**
```tsx
<FeaturedSection>
  <SectionTitle>Featured Adventures</SectionTitle>
  <ExperienceGrid>
    {featuredExperiences.map(exp => (
      <ExperienceCard>
        <ExperienceImage />
        <ExperienceTitle />
        <ExperienceLocation />
        <ExperiencePrice />
        <ExperienceRating />
      </ExperienceCard>
    ))}
  </ExperienceGrid>
</FeaturedSection>
```

**Note**: Label as "adventures" not "experiences" per requirements
**Condition**: Only implement if easy to do

### 1.5 Homepage Copy Updates (P0)

#### **Hero Section Text Changes**
```tsx
// Current text needs updating
<HeroTitle>
  "Dive into your next [sailing/diving/kiteboarding/surfing/kayaking/sea] adventure"
</HeroTitle>
```

**Implementation**: Make the activity type dynamic/rotating

#### **Trust & Safety Section (P0)**
```tsx
<TrustSection>
  <TrustItem>
    <Icon>✓</Icon>
    <Title>Verified Hosts</Title>
    <Description>Verified businesses</Description>
  </TrustItem>
  <TrustItem>
    <Icon>🌍</Icon>
    <Title>Global Coverage</Title>
    <Description>Insurance included</Description>
  </TrustItem>
  <TrustItem>
    <Icon>🎯</Icon>
    <Title>Perfect Match</Title>
    <Description>Join thousands who have discovered their perfect water experience</Description>
  </TrustItem>
</TrustSection>
```

---

## 🔍 2. Search Results Page (P0)

### 2.1 Search Results Layout

#### **Page Structure**
```tsx
<SearchResultsPage>
  <SearchHeader>
    <SearchBarCompact /> {/* Simplified version of homepage search */}
    <ResultsCount>"{searchQuery}" - {count} results found</ResultsCount>
  </SearchHeader>
  
  <ContentArea>
    <FilterSidebar /> {/* Desktop only */}
    <ResultsGrid>
      {experiences.map(exp => <ExperienceCard />)}
    </ResultsGrid>
  </ContentArea>
  
  <MobileFilters /> {/* Mobile only - bottom sheet */}
</SearchResultsPage>
```

### 2.2 Experience Cards in Results

#### **Card Component Structure**
```tsx
<ExperienceResultCard>
  <ImageSection>
    <ExperienceImage />
    <ActivityTypeBadge>{activityType}</ActivityTypeBadge>
  </ImageSection>
  
  <ContentSection>
    <ExperienceTitle />
    <HostName />
    <LocationWithIcon />
    <PriceAndDuration />
    <RatingAndReviews />
    <BookingButton>View Details</BookingButton>
  </ContentSection>
</ExperienceResultCard>
```

**Visual Requirements:**
- **Layout**: 1 column on mobile, 2-3 columns on desktop
- **Image**: 16:9 aspect ratio, high quality
- **Typography**: Clear hierarchy with title, location, price
- **CTA**: Prominent "View Details" or "Book Now" button

### 2.3 Filter System (P1)

#### **Filter Categories**
```tsx
<FilterSidebar>
  <FilterSection title="Activity Type">
    <CheckboxGroup options={activityTypes} />
  </FilterSection>
  
  <FilterSection title="Price Range">
    <PriceRangeSlider />
  </FilterSection>
  
  <FilterSection title="Duration">
    <DurationSelector />
  </FilterSection>
  
  <FilterSection title="Location">
    <LocationFilter />
  </FilterSection>
  
  <FilterSection title="Date">
    <DateRangePicker />
  </FilterSection>
</FilterSidebar>
```

**Implementation Priority**: P1 - Implement if straightforward

---

## 📄 3. Experience Detail Page (P0)

### 3.1 Experience Header

#### **Header Section**
```tsx
<ExperienceHeader>
  <ImageGallery>
    <MainImage />
    <ThumbnailGrid />
  </ImageGallery>
  
  <ExperienceInfo>
    <ActivityTypeBadge />
    <ExperienceTitle />
    <HostProfile>
      <HostAvatar />
      <HostName />
      <HostRating />
    </HostProfile>
    <LocationWithMap />
    <PriceStartingFrom />
  </ExperienceInfo>
</ExperienceHeader>
```

### 3.2 Booking Widget (P0)

#### **Sticky Booking Panel**
```tsx
<BookingWidget>
  <PriceDisplay>
    <Price>€{price}</Price>
    <PriceUnit>per person</PriceUnit>
  </PriceDisplay>
  
  <DateSelector />
  <GuestSelector />
  <TotalCalculation />
  
  <BookingButton>
    Reserve Experience
  </BookingButton>
  
  <BookingNote>
    You won't be charged yet
  </BookingNote>
</BookingWidget>
```

**Positioning**: Sticky on desktop, bottom fixed on mobile

### 3.3 Experience Details Tabs

#### **Content Sections**
```tsx
<ExperienceContent>
  <TabNavigation>
    <Tab>Overview</Tab>
    <Tab>What's Included</Tab>
    <Tab>Location</Tab>
    <Tab>Reviews</Tab>
    <Tab>Host</Tab>
  </TabNavigation>
  
  <TabContent>
    <OverviewTab>
      <Description />
      <Highlights />
      <WhatToBring />
    </OverviewTab>
    {/* Other tabs */}
  </TabContent>
</ExperienceContent>
```

---

## 🛒 4. Booking Flow (P0)

### 4.1 Booking Modal/Page

#### **Booking Form Steps**
```tsx
<BookingFlow>
  <StepIndicator currentStep={1} totalSteps={3} />
  
  <Step1_ExperienceDetails>
    <SelectedExperience />
    <DateAndGuests />
    <PriceBreakdown />
  </Step1_ExperienceDetails>
  
  <Step2_GuestInfo>
    <ContactForm />
    <SpecialRequests />
  </Step2_GuestInfo>
  
  <Step3_Payment>
    <PaymentForm />
    <BookingSummary />
    <TermsAndConditions />
  </Step3_Payment>
</BookingFlow>
```

### 4.2 Confirmation Page

#### **Booking Success**
```tsx
<BookingConfirmation>
  <SuccessIcon />
  <ConfirmationMessage>
    Your adventure is confirmed!
  </ConfirmationMessage>
  
  <BookingDetails>
    <ExperienceSummary />
    <BookingReference />
    <ContactInstructions />
  </BookingDetails>
  
  <NextSteps>
    <HostContact />
    <CalendarAdd />
    <DirectionsLink />
  </NextSteps>
</BookingConfirmation>
```

---

## 📱 5. Responsive Design Requirements

### 5.1 Mobile-First Implementation

#### **Breakpoints**
```css
/* Mobile First */
.container {
  padding: 16px;
}

/* Tablet */
@media (min-width: 768px) {
  .container {
    padding: 24px;
  }
}

/* Desktop */
@media (min-width: 1024px) {
  .container {
    padding: 32px;
    max-width: 1200px;
    margin: 0 auto;
  }
}
```

### 5.2 Mobile-Specific Features

#### **Mobile Navigation**
```tsx
<MobileHeader>
  <Logo />
  <SearchIcon onClick={openSearchModal} />
  <MenuIcon onClick={openMobileMenu} />
</MobileHeader>

<MobileSearchModal>
  <SearchBar />
  <RecentSearches />
  <PopularActivities />
</MobileSearchModal>
```

---

## 🎨 6. Design System Specifications

### 6.1 Color Palette

#### **Primary Colors**
```css
:root {
  --primary-blue: #2563eb;
  --primary-teal: #0d9488;
  --primary-emerald: #059669;
  
  --secondary-cyan: #0891b2;
  --secondary-blue-light: #3b82f6;
  
  --accent-yellow: #f59e0b;
  --accent-orange: #ea580c;
}
```

### 6.2 Typography

#### **Font Hierarchy**
```css
/* Headers */
.hero-title {
  font-size: clamp(2.5rem, 8vw, 4rem);
  font-weight: 800;
  line-height: 1.1;
}

.section-title {
  font-size: clamp(1.75rem, 4vw, 2.5rem);
  font-weight: 700;
}

/* Body Text */
.body-large {
  font-size: 1.125rem;
  line-height: 1.6;
}

.body-regular {
  font-size: 1rem;
  line-height: 1.5;
}
```

### 6.3 Component Styling

#### **Button Styles**
```css
.btn-primary {
  background: linear-gradient(135deg, var(--primary-blue), var(--primary-teal));
  color: white;
  padding: 12px 24px;
  border-radius: 8px;
  font-weight: 600;
  transition: transform 0.2s, box-shadow 0.2s;
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
}
```

#### **Card Styles**
```css
.experience-card {
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  transition: transform 0.3s, box-shadow 0.3s;
}

.experience-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
}
```

---

## ✅ 7. Implementation Checklist

### 7.1 Phase 1 - P0 Features (Must Have)

#### **Homepage**
- [ ] Hero section with dynamic activity text
- [ ] Search bar with 3 fields (What, Where, When)
- [ ] Activity categories grid (6 activities)
- [ ] Trust & safety section with updated copy
- [ ] Mobile responsive layout

#### **Search Results**
- [ ] Search results page layout
- [ ] Experience cards with all required info
- [ ] Basic filtering (if easy to implement)
- [ ] Pagination or infinite scroll

#### **Experience Details**
- [ ] Experience detail page layout
- [ ] Image gallery
- [ ] Booking widget (sticky/fixed)
- [ ] Experience information tabs
- [ ] Host profile section

#### **Booking Flow**
- [ ] Booking form/modal
- [ ] Multi-step process
- [ ] Payment integration
- [ ] Confirmation page
- [ ] Email confirmations

### 7.2 Phase 2 - P1 Features (Nice to Have)

#### **Enhanced Search**
- [ ] Advanced filters sidebar
- [ ] Map view integration
- [ ] Suggested categories in search
- [ ] Suggested locations

#### **Content Enhancements**
- [ ] Destination showcase
- [ ] Featured adventures section
- [ ] Enhanced image galleries
- [ ] Video content

### 7.3 Phase 3 - P2 Features (Future)

#### **Advanced Features**
- [ ] Real-time chat
- [ ] Advanced analytics
- [ ] Multi-language support
- [ ] Mobile app

---

## 📋 8. Technical Implementation Notes

### 8.1 Performance Requirements

#### **Loading Times**
- Homepage: < 3 seconds
- Search results: < 2 seconds
- Experience details: < 2 seconds
- Booking flow: < 1 second per step

#### **Optimization**
- Image lazy loading
- Code splitting by route
- API response caching
- Progressive image loading

### 8.2 SEO Requirements

#### **Meta Tags**
```tsx
<Head>
  <title>SeaFable - Book Water Adventures Worldwide</title>
  <meta name="description" content="Discover and book amazing water adventures..." />
  <meta property="og:title" content="SeaFable" />
  <meta property="og:description" content="..." />
  <meta property="og:image" content="/og-image.jpg" />
</Head>
```

### 8.3 Accessibility Requirements

#### **WCAG 2.1 AA Compliance**
- Keyboard navigation support
- Screen reader compatibility
- Color contrast ratios > 4.5:1
- Alt text for all images
- Semantic HTML structure

---

## 🚀 9. Launch Strategy

### 9.1 MVP Launch Criteria

#### **Minimum Viable Features**
1. ✅ Working search functionality
2. ✅ Experience browsing and details
3. ✅ Booking flow completion
4. ✅ Payment processing
5. ✅ Mobile responsiveness
6. ✅ Basic security measures

### 9.2 Success Metrics

#### **Technical KPIs**
- Page load speed < 3s
- Mobile usability score > 90
- Zero critical bugs
- 99% uptime

#### **User Experience KPIs**
- Search-to-booking conversion > 5%
- Mobile traffic > 60%
- User session duration > 2 minutes
- Bounce rate < 40%

---

This specification provides the detailed implementation guidelines needed to build the SeaFable MVP according to the requirements, with clear prioritization and technical details for the development team.